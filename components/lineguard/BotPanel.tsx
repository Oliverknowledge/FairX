/**
 * Back-compat alias. The elemental BotPanel was upgraded into the real actor
 * SniperBotPanel (side toggle + escrow-aware economics). Kept as a re-export so
 * the original name still resolves.
 */
export { SniperBotPanel as BotPanel } from "@/components/lineguard/SniperBotPanel";
